import { primaryColor } from './Variables.style';

export const globalColor = {
  bgBtnPrimary: primaryColor.gravy,
  bgBtnSecondary: primaryColor.bears,
  bgBtnSocial: primaryColor.bears,
  bgBtnSocialHover: primaryColor.roses,
  textBtnPrimary: primaryColor.white,
  textBtnSecondary: primaryColor.close,
  iconSocial: primaryColor.roses,
  iconSocialHover: primaryColor.white
};

export const headerColor = {
  bg: primaryColor.white,
  textLogo: primaryColor.roses,
  link: primaryColor.shaft,
  linkHover: primaryColor.roses,
  border: primaryColor.hazzy
};

export const homeColor = {
  textTheme: primaryColor.roses,
  textDescription: primaryColor.happy,
  textDescriptionDark: primaryColor.looks,
  textHeaderHero: primaryColor.white,
  textHeaderLocation: primaryColor.close,
  textHero: primaryColor.white,
  textCountdown: primaryColor.shaft,
  textShadow: primaryColor.black,
  bgIconHero: primaryColor.roses,
  bgIconCouple: primaryColor.black,
  bgCountdown: primaryColor.hazzy,
  bgHero: primaryColor.ebony,
  bgDark: primaryColor.hazzy,
  bgTheme: primaryColor.roses,
  borderCircle: primaryColor.skill,
  iconCouple: primaryColor.shaft,
  iconTitle: primaryColor.skill,
  iconLocation: primaryColor.white
};

export const footerColor = {
  bg: primaryColor.ebony,
  link: primaryColor.roses,
  textName: primaryColor.roses,
  textPhone: primaryColor.stone,
  textCopyright: primaryColor.looks,
  icon: primaryColor.white
};